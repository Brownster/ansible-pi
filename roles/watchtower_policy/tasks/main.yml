---
# Configure Watchtower update policy

- name: Ensure watchtower config directory exists
  file:
    path: "{{ mediastack_root }}/watchtower"
    state: directory
    owner: "{{ main_user }}"
    group: "{{ main_group }}"
    mode: '0755'

- name: Render watchtower env file
  template:
    src: watchtower.env.j2
    dest: "{{ mediastack_root }}/watchtower/watchtower.env"
    owner: "{{ main_user }}"
    group: "{{ main_group }}"
    mode: '0644'
